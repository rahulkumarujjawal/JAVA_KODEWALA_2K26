package com.kodewala.abstracts;

public class Account {

	/*
	 * 
	 * variables --> hold the  values / state i.g ----> balance = 120000;
	 * +
	 * 
	 * Methods(What class will do ) --> task or work -->
	 * Method will use the balance ad perform  the fund transfer
	 * balance  =  balance -some amount 
	 * balance = balance + some amount 
	 * 
	 * 
	 * 
	 * Method and Implementation 
	 * 
	 * Abstract class === > it means what do be done .
	 */
	
	
	public void productDetails() {     // task (what and how )    //    This is a variables and  method inside the class 
	
		// doing productDeatils 
	}
}
